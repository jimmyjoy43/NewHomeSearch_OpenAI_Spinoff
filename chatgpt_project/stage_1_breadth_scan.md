# Stage 1: Breadth Scan

Read the uploaded project files first.
Then browse for candidate buildings that fit the current brief.

Return:
1. a short findings summary,
2. the full replacement contents for `state/buildings.csv`,
3. any new rows that should also go into `state/decisions.csv` if a building is immediately rejected.

Rules:
- one row per building
- do not create unit rows yet
- use `unknown` rather than guessing
- note any immediate hard-stop risks in the `notes` field
